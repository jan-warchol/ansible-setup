# thanks to multicast DNS I can simply use hostnames to find my machines,
# regardless of the network they are using and IPs they were assigned.
# https://blog.doismellburning.co.uk/handling-local-hostnames-multicast-dns/

jw-t430s  ansible_user=jan ansible_host=jw-t430s.local
