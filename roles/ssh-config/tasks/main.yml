- name: ensure .ssh directory exists
  file:
    path: ~/.ssh
    state: directory

- name: update ssh config
  copy:
    src: ssh-config-{{ ansible_env.USER }}
    dest: ~/.ssh/config
    backup: yes

- name: add my personal key to authorized keys
  lineinfile:
    dest: ~/.ssh/authorized_keys
    line: "{{ item }}"
    create: yes
  with_items:
    - "{{ id_rsa_personal_2c6819cb_public_key }}"
    - "{{ id_rsa_codilime_5260631d_public_key }}"
