# apparently I cannot put the same host with different users in different groups,
# so I had to create an "jwlap-cl" alias for my machine (see /etc/hosts).

[personal_computers]
jwlap ansible_ssh_user=janek

[work_computers]
jwlap-cl ansible_ssh_user=codilime

[test_vagrant_vms]
test-vm-1 ansible_ssh_user=vagrant
